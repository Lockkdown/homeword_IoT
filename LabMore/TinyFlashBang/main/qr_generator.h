#pragma once

/**
 * @brief Generate and print QR code for device provisioning
 * 
 * This function generates a QR code containing the device ID and type,
 * then prints it to the serial console as ASCII art.
 */
void generate_device_qr(void);
